import { View, Text } from 'react-native';
import React from 'react';

const Notification = () => {
  return (
    <View style={{ flex:1, justifyContent:"center", alignItems:'center'}}>
      <Text style={{ fontFamily:'Nunito' }}>No Notifications Yet</Text>
    </View>
  )
};

export default Notification;